-- 데드코드·미사용 테이블 감사(2026-08-16)의 유일한 DDL 산출물.
--
-- 감사 결과 실제로 지울 테이블은 **1개뿐**이다. 후보로 뽑혔던 나머지 28종은 전부
-- PK 명명규칙 통일(20260726115611)로 단수형이 된 옛 이름이거나, 이미 앞선
-- 마이그레이션이 DROP 한 것이다(20260726110105 *_bak, 20260727104951 saved_view,
-- 20260730120000 remediation/sla, 20260730173000 host_ext_port). 판별 근거는
-- docs/dev/cleanup-evidence.md 의 "2026-08-16 데드코드·DB 감사" 절에 표로 있다.
--
-- tb_activity_review — 접속기록 "월 1회 점검" 기능과 함께 폐기됐다.
--   폐기 자체는 20260813001657_drop_activity_review.sql 이 이미 했고 그 파일은 공용 dev DB 에
--   2026-08-13 00:21:38 에 적용됐다. 그런데 실측하니 같은 날 00:46:16 생성시각으로 테이블이
--   **다시 생겨 있었다**(0행). 적용 이력에 없는 경로로 되살아난 dev 잔재다.
--   migrate.sh 는 적용 기록이 있는 파일을 다시 돌리지 않으므로, 지우려면 새 파일이 필요하다.
--   운영에는 이미 없으므로 이 문장은 무동작이다(멱등).
--
--   되살아나도 무해하다: 읽고 쓰는 코드가 저장소 어디에도 없고
--   (server/src/compliance.php 의 주석 1줄과 tests/documentation_consistency_test.php 의
--    "폐기됨" 단언이 전부), 열린 PR·원격 브랜치 3개 모두 이 테이블을 쓰지 않는다.
SET NAMES utf8mb4;

DROP TABLE IF EXISTS tb_activity_review;
