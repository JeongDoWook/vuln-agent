# 기본: 텍스트는 git이 자동 정규화
* text=auto

# 쉘 스크립트는 리눅스 대상에서 실행 → LF 고정 (CRLF면 실행 오류)
*.sh   text eol=lf

# 서버측 텍스트도 LF 로 통일 (컨테이너=리눅스)
*.php  text eol=lf
*.sql  text eol=lf
*.yml  text eol=lf
*.cnf  text eol=lf
Dockerfile text eol=lf
Caddyfile  text eol=lf
