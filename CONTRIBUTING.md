# 기여 가이드

vuln-agent 는 설치된 취약점의 개수가 아니라 지금 이 서버에서 실제로 위험한 것을 먼저
보여주는 경량 취약점 진단 플랫폼입니다. 이 문서는 이 저장소에 기여하는 방법을 안내합니다.

이 프로젝트는 **1인 유지보수** 체제입니다. 대규모 컨트리뷰터 조직이나 코드 오너 팀은
없고, 이슈·PR 은 유지보수자가 직접 검토합니다. 응답이 늦어질 수 있는 점을 양해해 주세요.

## 로컬에서 띄우고 검증하기

```bash
cd deploy && ./compose_runner.sh init && ./compose_runner.sh doctor && ./compose_runner.sh dev up -d
```

웹은 <http://localhost:8000> 에서 뜹니다(로컬에 PHP·MySQL 설치가 필요 없습니다). 자세한 내용은
README 의 ["빠르게 실행해 보기"](README.md#빠르게-실행해-보기)와 [배포 가이드](deploy/README.md)를
참고하세요.

변경한 코드는 push 전에 검증합니다:

```bash
./tests/smoke.sh http://localhost:8000
```

이 스크립트는 `tests/ui_lint.sh`(정적 UI 검사)와 `tests/vercmp_test.php`(버전 비교 단위 테스트)를
먼저 돌린 뒤 수집·매칭·인증·화면을 curl 기반으로 점검합니다. PHP·쉘 스크립트를 고쳤다면
`php -l`·`bash -n` 도 통과해야 합니다. 같은 검증이 [`.github/workflows/ci.yml`](.github/workflows/ci.yml)
로 CI 에서도 돕니다.

## 기여 방법

1. 먼저 [이슈](../../issues)로 무엇을 하려는지 제안해 주세요. 버그는 버그 리포트 템플릿을,
   새 기능은 기능 제안 템플릿을 씁니다. 작은 오타·문서 수정은 바로 PR 을 열어도 괜찮습니다.
2. 저장소를 포크하고 작업 브랜치를 만듭니다(`feat/`·`fix/`·`chore/` 접두사를 권장합니다).
3. 변경하고, 위 검증을 통과시킨 뒤 커밋합니다. 커밋 메시지는 한글로, "무엇을·왜"가 드러나게
   씁니다(예: `수정: 패키지 목록 페이지네이션이 총건수를 안 보여주던 문제`).
4. PR 을 엽니다. PR 템플릿의 검증 체크리스트를 실제로 돌린 것만 체크해 주세요.

## 코드 스타일

- **YAGNI/KISS** — 지금 필요한 것만, 가장 단순한 방법으로 만듭니다. 나중에 쓸지도 모른다는
  이유로 추상화·설정 옵션을 미리 만들지 않습니다.
- **DRY, 단 성급하지 않게** — 반복되는 로직은 헬퍼로 뽑되, 2회까지는 허용하고 3회째에
  추출합니다(`server/src/`에 이미 있는 헬퍼를 먼저 찾아보세요).
- **SOLID** — 파일·함수는 한 책임만 지고, 새 기능은 기존 코드를 고치지 않고 확장하는 방향을
  우선합니다.
- **시큐어 코딩** — SQL 은 PDO prepared statement 만 씁니다(문자열 조합 쿼리 금지). 사용자
  입력이 섞인 출력은 반드시 이스케이프합니다(`vg_h()`). 인가는 서버 측에서 확정합니다.
- 매직 넘버·URL·경로는 설정·DB 로 뺍니다. 단, 바뀔 일이 없는 고정 피드 스키마처럼 KISS 상
  하드코딩이 더 단순한 경우는 예외로 둡니다.

## 라이선스

이 저장소는 [AGPL-3.0](LICENSE) 으로 배포됩니다. 기여한 코드도 같은 라이선스로 배포되는 데
동의하는 것으로 간주합니다.

## 보안 취약점 발견 시

이 저장소에 보안 취약점을 발견했다면 **공개 이슈로 등록하지 말고**, 저장소의
[Security 탭 → "Report a vulnerability"](../../security/advisories/new)(GitHub Private Security
Advisory)로 알려주세요. 비공개로 유지보수자에게만 전달되고, 이슈보다 안전하게 조치할 시간을
벌 수 있습니다. 이 기능을 쓸 수 없는 상황이라면 일반 이슈로 최소한의 정보만 남겨 주세요.
