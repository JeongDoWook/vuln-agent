# =============================================================================
# vuln-agent · 에이전트 배포 대상 노드 목록 (템플릿, 실제 주소 없음)
# =============================================================================
# 이 파일을 agent_nodes.txt 로 복사해 실제 노드를 적는다.
#   cp deploy/agent_nodes.txt.template deploy/agent_nodes.txt
# agent_nodes.txt 는 절대 커밋하지 않는다(.gitignore) — 내부망 인벤토리가 그대로
# 공개되기 때문이다. 이 .template 만 커밋한다(.env.prod.template 과 같은 규약).
#
# 형식: 한 줄에 하나, `[사용자@]호스트`. '#' 뒤는 주석, 빈 줄은 무시.
#   사용자를 생략하면 AGENT_SSH_USER(기본 worker)를 쓴다.
# 쓰는 곳: deploy/install_staged_agents.sh (인자 없이 실행할 때의 대상 목록)
# =============================================================================

10.0.0.100
10.0.0.101
10.0.0.102
deploy@10.0.0.200
